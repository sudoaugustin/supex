import codes from "./codes";
import Editor from "components/Editor";

# worker.js

The service worker to execute since the browser startup and handle events until the browser is closed or extension is disabled or removed. Learn more [here](https://developer.mozilla.org/en-US/docs/Mozilla/Add-ons/WebExtensions/Background_scripts).

<Editor code={codes} filename="app/worker.js" />
