import codes from "./codes";
import Editor from "components/Editor";

# Public Folder

By default Supex will only include the build output of `app` folder in extension output. If you want to include an image or video in your extension output, please put them inside `public` folder.

![](/images/public-folder.png)

<Editor code={codes.action} sync="framework" filename="app/action.js" />
